stages:
- stage:
  jobs:
  - template: templates/jobs.yml  
    parameters:
      repositories: 
      - name: application_web
        job: applicationweb
      - name: application_api
        job: applicationapi